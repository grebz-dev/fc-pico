/**
   Copyright (c) 2025 impact soft

*/

/**
 * @file tuto1_hw.ino
 * @brief Arduino sketch entry point for the FC PICO firmware.
 * @ingroup platform
 *
 * Intentionally empty. The firmware is a **unity build**: every translation unit
 * is pulled in textually by system.h, which also supplies the `setup()`/`loop()`
 * pair for core 0 and `setup1()`/`loop1()` for core 1.
 *
 * @note Adding a new `.cpp` under `sys/` requires adding it to system.h; the
 *       Arduino IDE will not compile it on its own.
 * @see @ref architecture, @ref dev_setup
 */


#include "system.h"
