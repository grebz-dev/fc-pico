# SPDX-License-Identifier: BSD-3-Clause
#
# fcbus -- cartridge bus library. Backend-independent core plus a host simulation
# backend today; mirrors tutorial_project/tuto1_hw/sys/rp_system.{cpp,h} (see README.md).

add_library(fcbus_core STATIC
    fcbus_core.c
)
target_include_directories(fcbus_core PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})

add_library(fcbus_host STATIC
    fcbus_host.c
)
target_link_libraries(fcbus_host PUBLIC fcbus_core)
target_include_directories(fcbus_host PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})

# TODO(P0-T5): device backend -- PIO/DMA/IRQ glue over fcbus_core, mirroring
# rp_system::init() / ppu_dma() / jobRcvCom() / rom_dma() / ver_dma() / drq_ret(), with the
# ISR and DMA re-arm path in RAM (__not_in_flash_func). Needs pico-sdk (hardware_pio,
# hardware_dma, hardware_irq) and the fcppu.pio header generated by pioasm at build time;
# only buildable once FCPICO_HOST_ONLY is OFF (doom/plan/10-workplan.md, P0-T5).
#
# add_library(fcbus STATIC fcbus_device.c)
# target_link_libraries(fcbus PUBLIC fcbus_core hardware_pio hardware_dma hardware_irq)
# pico_generate_pio_header(fcbus ${CMAKE_CURRENT_SOURCE_DIR}/fcppu.pio)
